import React from 'react';
import { View, Button } from 'react-native';

const NavigationBar = () => (
  <View>
    <Button title='Single answer' />
    <Button title='Multiple answers' />
    <Button title='Create new card' />
    <Button title='Login' />
  </View>
);

export default NavigationBar;
