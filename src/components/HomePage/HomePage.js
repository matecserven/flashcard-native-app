import React from 'react';
import NavigationBar from '../NavigationBar/NavigationBar';
import { StyleSheet, Text, View } from 'react-native';

const HomePage = () => (
  <View>
    <Text>Welcome to the Flash Card App</Text>
    <NavigationBar />
  </View>
);

export default HomePage;
